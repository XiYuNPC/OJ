# WebOJ Harness（约束套件）

本文件是本仓库的 harness：对**人**与 **AI 助手**同时生效的规则、范围与核对办法。人和助手开始任何改动前先读本文件；AI 助手在每项任务开始前须依据规则 1 先列改动清单征得确认，任务结束时按规则 4 核对后再宣布完成。

## 规则

1. **范围（改哪里、怎么改）**：一次任务开始前先列出改动清单（涉及文件 + 改动要点），征得确认后再动手（修改前先问）；改动只允许落在与任务直接相关的 `server/`、`public/`、`scripts/`、`docs/`、`README.md` 文件，禁止顺手改无关文件。
2. **禁区（不能动哪里）**：`data/`（运行时数据库与评测机密钥）与 `node_modules/` 不可改动；自测一律用内存数据库，不污染演示数据；`docs/plan.md`（立项书）与已交报告的实质口径不可随意改动；文档与报告不得出现密钥/机密、仓库地址、录屏外链（扫描口径见 `scripts/harness-check.js`）。
3. **依赖与密钥**：不新增 npm 依赖（选型锁定 Node 内置 `node:sqlite`、express、jsonwebtoken、ws、emception）；评测机密钥由 `data/.judge-key` 首次启动自动生成，禁止写死进代码或文档。
4. **完成标准（怎样算做完）**：① `npm test` 全部断言通过（当前 136 项）；② 涉及演示数据时 `npm run seed` 幂等；③ 接口 / 判题口径变更同步 `docs/api.md` 与 `README.md`；④ 浏览器端到端走查已执行，或明确记录为「待用户本地执行」（工作区无法运行浏览器时允许后者）；⑤ 涉及的设计文档 / 报告同步更新；⑥ 判题口径变更时复核 MLE/RE 等兜底分支条件非恒真（2026-08-25 B8 曾因 INITIAL_MEMORY=题限 使「HEAPU8 ≥ 90% 题限」兜底恒真、把崩溃误判 MLE）。
5. **提交**：`docs/` 与各天报告不入 git；git 提交命令整理成清单后交用户本地执行，AI 助手不直接提交、不改写 git 历史。
6. **AI 协作**：报告中的 AI 沟通节据实记录真实协作过程，不编造案例。

## 核对办法

- `npm test`（136 项断言）、`npm run seed`、`npm run stress`、`npm run verify-p6838`（通信题标号方案数据互验）；
- `npm run harness`：扫描 `docs/` 与 `README.md` 中的仓库地址与密钥特征（`scripts/harness-check.js`，违规输出「文件:行号」并以非零退出码告警）；
- 浏览器走查清单见各设计文档的自测计划（如 `docs/P6838-网络站点评测方案.md` §9 的 7 个提交变体）。
